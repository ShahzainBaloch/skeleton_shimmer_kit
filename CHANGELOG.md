## 1.0.1

* Added comprehensive documentation, setup guides, and modular UI recipe examples.
* Refined shimmer sweep synchronization across nested bone structures.

## 1.0.0

* Initial release of `skeleton_shimmer_kit`.
* Synchronized shimmering wave scope (`SkeletonShimmer`).
* Atomic placeholder primitives (`SkeletonBone`, `SkeletonBone.circle`).
* Ready-to-use presets (`SkeletonAvatar`, `SkeletonParagraph`, `SkeletonListTile`, `SkeletonCard`).
* Zero external dependencies.
