name: skeleton_shimmer_kit
description: A comprehensive skeleton loading kit with synchronized shimmer animations, pre-built layout presets, and custom bone primitives.
version: 1.0.0
repository: https://github.com/ShahzainBaloch/skeleton_shimmer_kit
issue_tracker: https://github.com/ShahzainBaloch/skeleton_shimmer_kit/issues
topics:
  - ui
  - shimmer
  - skeleton
  - loading
  - placeholder

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
